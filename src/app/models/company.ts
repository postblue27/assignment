export class Company {

    constructor(public id: string, public name: string, public price: number, public sector: string, public industry: string, public employees: number, public region_id: string) { }
}